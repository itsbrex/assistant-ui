---
"@assistant-ui/react-native": patch
---

feat: let `ComposerPrimitive.AddAttachment` take an `onPress` handler, so the platform picker runs behind the composer's disabled state instead of on a separate button
