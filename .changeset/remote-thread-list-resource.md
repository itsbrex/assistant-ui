---
"@assistant-ui/core": patch
"@assistant-ui/react": patch
---

feat: add a `RemoteThreadList` store entry so any `AssistantClient` host can run a remote thread list from a `RemoteThreadListAdapter` and a `thread` factory
