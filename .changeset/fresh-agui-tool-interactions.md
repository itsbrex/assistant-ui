---
"@assistant-ui/react-ag-ui": patch
---

feat: the AG-UI runtime records tool interactions on their tool call, keeps them while the run streams and A2UI surfaces are rebuilt, and writes them to history
