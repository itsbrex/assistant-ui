---
"@assistant-ui/react-generative-ui": patch
---

feat: add a `CheckboxGroup` vocabulary component for picking any number of options; its `$input` is the array of checked values, a `Form` collects it the same way, Slack renders it as `checkboxes`, and Teams as a multi-select `Input.ChoiceSet`
