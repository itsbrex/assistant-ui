---
"@assistant-ui/react-a2a": patch
---

feat: A2UI DataParts render as surfaces whose tool artifact replays them, the task's artifacts ride on the assistant message in `metadata.custom.a2a.artifacts` without inline file bytes, and a message paused for input is written to history with its question
