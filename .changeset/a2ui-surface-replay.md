---
"@assistant-ui/react-generative-ui": patch
---

feat: `surfaceToOperations` turns a reduced A2UI surface into operations that replay it, `convertSurfaceToUISpec` can keep unknown components with `keepUnknownComponents`, and `Icon`, `List`, `ChoicePicker` and `DateTimeInput` now render
