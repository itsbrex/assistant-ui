---
"@assistant-ui/react-generative-ui": patch
---

feat: an action fired in `present` or `prompt_user` output is recorded on its tool call, and a value returned by a `prompt_user` action handler becomes the tool result
