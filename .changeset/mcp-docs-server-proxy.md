---
"@assistant-ui/mcp-docs-server": minor
---

feat: proxy the hosted assistant-ui mcp endpoint instead of bundling a docs snapshot. the stdio server now forwards to the hosted tool set, and the programmatic `server` export is removed
