---
"@assistant-ui/react-native": patch
---

feat: `ThreadPrimitive.MessagesFlatList` keeps the visible message anchored while older messages load above it (`maintainVisibleContentPosition` defaults to the first visible item and stays overridable)
